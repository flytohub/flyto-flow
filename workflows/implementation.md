# Implementation Workflow

1. Change the smallest owning source area and preserve local-first defaults.
2. Delegate execution behavior to the installed `flyto-core` package.
3. Add focused tests and update source-backed feature documentation.
4. Update `FLOW_CLOUD_SYNC.json` only when the complete shared-file contract
   deliberately changes.
