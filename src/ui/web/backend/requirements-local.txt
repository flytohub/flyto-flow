# Flyto2 Local Runner Dependencies (Desktop / Tauri sidecar)
#
# Excludes cloud-only deps: firebase-admin, google-*, stripe, Pillow, passlib/bcrypt.
# Cloud routes are proxied to CLOUD_API_URL, not handled locally.

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.18

# Async HTTP (proxy to cloud + httpx for outbound)
httpx>=0.26.0
aiohttp>=3.14.0

# WebSocket client (real-time screencast push to cloud relay)
websockets>=12.0

# Data validation
pydantic[email]>=2.5.0
email-validator>=2.1.0

# Security (token format validation, credential encryption)
PyJWT>=2.10.1,<3.0.0
cryptography>=46.0.6

# Utilities
python-dotenv>=1.0.0
PyYAML>=6.0.1
tenacity>=8.2.0

# AI LLM Providers (BYOK chat)
openai>=1.12.0
anthropic>=0.39.0

# Flyto2 Core (execution engine + Playwright)
flyto-core>=2.25.26
